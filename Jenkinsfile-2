pipeline {
  agent any

  stages {
    stage('Hello') {
      steps {
        sh 'ansible-galaxy collection install -r requirements.yml'
        ssh 'ansible-playbook -i inventory/mariadb.hosts playbooks/mariadb.yml'
      }
    }
  }
}
