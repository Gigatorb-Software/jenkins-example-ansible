pipeline {
  agent any

  stages {
    stage('Hello') {
      steps {
        sh 'ansible-galaxy collection install -r requirements.yml'
        sh 'ansible-playbook --private-key=$ANSIBLE_PRIVATE_KEY playbooks/mariadb.yml -inventory.ini -e "ansible_become_password=p@$$w0rd"'
      }
    }
  }
}
